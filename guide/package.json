{
  "name": "guide",
  "version": "1.0.0",
  "description": "Guide.de — Anleitungs-Marktplatz",
  "private": true,
  "scripts": {
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "dev": "netlify dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "deploy": "netlify deploy --prod"
  }
}
